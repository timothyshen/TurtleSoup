{
  "name": "haiguitang-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Edge proxy for Claude API + Firebase Auth bridge (haiguitang macOS app)",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "jose": "^5.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.6.0",
    "vercel": "^37.0.0"
  }
}
